{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Auth Service Test",
      "type": "go",
      "request": "launch",
      "mode": "test",
      "program": "${workspaceFolder}/service/auth",
      "env": {
        "APP_ENV": "development"
      },
      "cwd": "${workspaceFolder}"
    }
  ]
}
